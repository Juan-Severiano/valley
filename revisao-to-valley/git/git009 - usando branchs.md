# Usando Branchs

ramificações da linha do tempo do seu projeto

```bash
# lista as branchs
git branch

# cria uma branch
git branch dev

# muda as branch
git checkout main

# deleta a branch
git branch -d dev
git branch -D dev
```
