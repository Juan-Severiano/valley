# Desfazendo Commits

```bash
# checkout - vc só ve oq passou na linha do tempo
git checkout nomedocommit
# revert - reverte uma linha do temnpo
git revert nomedocommit
# reset - acaba com tudo
git reset nomedocommit --hard
```
